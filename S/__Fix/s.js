class User {
    constructor(user) {
        this.user = user;
    }
}

class UserModel {
    getUser(userId) {
        // return user
    }
}

class UserController {
    saveUser(user) {
        // save user to database
    }
}